import { ConvertToUriPipe } from './convert-to-uri.pipe';

describe('ConvertToUriPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertToUriPipe();
    expect(pipe).toBeTruthy();
  });
});
